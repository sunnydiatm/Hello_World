This programs stores the first and second operand of a GCD into MongoDB. In the process, it calculate the GCD and store the results as well. Second operation fetches list of all stored GCD until now.
